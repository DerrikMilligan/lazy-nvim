-- To use tab instead of return to accept stuff we want to use the `supertab`
-- https://www.lazyvim.org/configuration/recipes#supertab
return {
  "L3MON4D3/LuaSnip",
  -- Disable the tab and shift tab behavior of LuaSnip
  keys = function()
    return {}
  end
}
